# Legacy client

This directory contains the retired HTML/JavaScript client for historical
reference. FastAPI no longer serves these files. The canonical client lives in
`frontend-react/`; build it with `npm ci` followed by `npm run build`.
