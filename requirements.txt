fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy[asyncio]
asyncpg
aiosqlite
httpx  # async client for the NPC dialogue provider (AI Power Grid)
bcrypt  # password hashing (ACCOUNTS.md Decision 3)
bcrypt  # password hashing (ACCOUNTS.md Decision 3)

# test
pytest
pytest-asyncio